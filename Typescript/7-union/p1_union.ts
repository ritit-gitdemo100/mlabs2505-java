//Union types are a powerful way to express
// a value that can be one of the several types. 

var empid:number|string

empid = 101
console.log(empid, typeof(empid))

empid = "RIT101"
console.log(empid, typeof(empid))

//empid = true
