/*
    Typescript Variables
        can have alphabets, numbers, _ and $.
        it cannot start with a number
        It cannot contain space or other special characters

    we can use var, let & const to declare variables
*/

var roll_no:number = 101;
var first_name:string = "Anand";
var isvalid:boolean = true;
var address:void = undefined; //void type can have only undefined or null, it is used with function
var stud_id:any = "ABC111";

console.log(roll_no);
console.log(first_name);
console.log(isvalid);
console.log(address);
console.log(stud_id);